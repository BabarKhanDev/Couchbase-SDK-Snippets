{
    "Create Collection": {
        "prefix": ["@cbmkcollection"],
        "body": ["CollectionSpec spec = CollectionSpec.create(\"${1:collectionName}\", \"${2:scopeName}\");","","try {", "  collectionMgr.createCollection(spec);","}","catch (CollectionExistsException e) {","  System.out.println(\"Collection already exists\");","}","catch (ScopeNotFoundException e) {","  System.out.println(\"The specified parent scope doesn't exist\");", "}"],
        "description": "Create a new collection, this requires a collection manager to aleady be present. You can make one with the snippet: @cbmkcollectionmanager"
    },
    "Create Scope": {
        "prefix": ["@cbmkscope"],
        "body": ["try {","  collectionMgr.createScope(\"${1:scopeName}\");", "}", "catch (ScopeExistsException e) {","  System.out.println(\"Scope already exists\");", "}"],
        "description": "Create a new scope, this requires a collection manager to aleady be present. You can make one with the snippet: @cbmkcollectionmanager"
    },
    "Create Bucket": {
        "prefix": ["@cbmkbucket"],
        "body": ["${1:cluster}.buckets()","  .createBucket(BucketSettings.create(\"${2:bucketName}\")","    .bucketType(BucketType.COUCHBASE)","    .ramQuotaMB($3)","    .numReplicas($4)","    .replicaIndexes($5)", "    .flushEnabled($6));"],
        "description": "Create a new bucket on your cluster object"
    },
    "Create Index": {
        "prefix": ["@cbmkindex"],
        "body": ["QueryIndexManager indexManager = QueryIndexManager()", "indexManager.createIndex(\"${1:bucketName}\", \"${2:indexName}\", ${3:fields})"],
        "description": "Create a new index"
    },
    "Create Collection Manager": {
        "prefix": ["@cbmkcollectionmanager"],
        "body": ["CollectionManager collectionMgr = ${1:bucket}.collections();"],
        "description": "Create a collection manager on your bucket object."
    },
    "Connect to a Cluster": {
        "prefix": ["@cbcnncluster"],
        "body": ["Cluster ${1:cluster} = Cluster.connect(\"${2:connectionString}\", \"${3:username}\", \"${4:password}\");"],
        "description": "Connect to a cluster and create a new Cluster object"
    },
    "Connect to a Bucket": {
        "prefix": ["@cbcnnbucket"],
        "body": ["Bucket ${1:bucket} = ${2:cluster}.bucket(\"${3:bucketName}\");"],
        "description": "Get a bucket reference, and create a new Bucket object, using a cluster object that you can create using @cbcnncluster"
    },
    "Connect to a Scope": {
        "prefix": ["@cbcnnscope"],
        "body": ["Scope ${1:scope} = ${2:bucket}.scope(\"${3:scopeName}\");"],
        "description": "Get a scope reference, and create a new Scope object, using a bucket object that you can create using @cbcnnbucket"
    },
    "Connect to a Collection": {
        "prefix": ["@cbcnncollection"],
        "body": ["Collection ${1:collection} = ${2:scope}.collection(\"${3:collectionName}\");"],
        "description": "Get a collection reference, and create a new Collection object, using a scope object that you can create using @cbcnnscope"
    }
}