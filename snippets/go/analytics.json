{
    "Create Analytics Query": {
        "prefix": ["@cbanalytics"],
        "body": ["results, err := ${1:cluster}.AnalyticsQuery(\"${2:query}\", nil)","if err != nil {","    panic(err)","}"],
        "description": "Use SQL++ to Query the Couchbase Cluster."
    },
    "Analytics Query With Parameters": {
        "prefix": ["@cbanalyticspara"],
        "body": ["results, err := ${1:cluster}.AnalyticsQuery(","    \"${2:query}\",","    &gocb.AnalyticsOptions{","        PositionalParameters: []interface{}{${3:parameters}}, // keep this line for positional parameters","        NamedParameters: map[string]interface{}{${3:parameters}}, // keep this line for named parameters","    },",")"],
        "description": "Use SQL++ to Query the Couchbase Cluster, this has all options available."
    }
}